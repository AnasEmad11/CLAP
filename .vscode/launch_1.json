{

    "version": "0.2.0",
    "configurations": [
        
        {
            "name": "Python: generate_data.py",
            "type": "python",
            "request": "launch",
            "program": "data/generate_data.py",
            "console": "integratedTerminal",
            "args" : ["-d", "cifar10","-a", "0.1", "-cn", "100"],
            "env": {
                "PYTHONPATH": "${workspaceFolder}" }
        }

    ]
}

